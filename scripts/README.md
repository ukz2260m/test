# scripts/

Packaging scripts (PyInstaller build for the Windows exe) land here as the
final implementation step, after every `src/edit_cli` module is implemented
and tested. See `docs/ARCHITECTURE.md`.
